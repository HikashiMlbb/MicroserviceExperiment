resources: [../../base]

configMapGenerator:
  - name: app-config
    behavior: replace
    env: resources/prod-config.env

secretGenerator:
  - name: app-secrets
    behavior: replace
    env: resources/prod-secrets.env

patches:
  - path: patches/replicas.yml

